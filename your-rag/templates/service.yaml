apiVersion: v1
kind: Service
metadata:
  name: {{ include "your-rag.fullname" . }}
  labels:
    {{- include "your-rag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8501
      protocol: TCP
      name: http
  selector:
    {{- include "your-rag.selectorLabels" . | nindent 4 }}
