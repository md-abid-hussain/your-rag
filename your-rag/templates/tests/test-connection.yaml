apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "your-rag.fullname" . }}-test-connection"
  labels:
    {{- include "your-rag.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "your-rag.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
